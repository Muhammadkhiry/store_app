import 'package:flutter/material.dart';

class UserDataTap extends StatelessWidget {
  const UserDataTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}